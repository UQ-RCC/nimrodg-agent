/*--------------------------------------------------------------------------
 * This file is autogenerated from config.cpp.in
 * during the cmake configuration of your project. If you need to make changes
 * edit the original file NOT THIS FILE.
 * --------------------------------------------------------------------------*/
#include "config.h"

#include <amqp.h>
#include <curl/curlver.h>
#include <uriparser/UriBase.h>

const compile_info_t g_compile_info = {
	.git = {
		.hash			= "@GIT_HASH@",
		.description	= "@GIT_DESCRIBE@",
	},
	.version = {
		.agent			= NIMRODG_VERSION_STRING,
		.openssl		= "@OPENSSL_VERSION@",
		.curl			= LIBCURL_VERSION,
		.uriparser		= URI_VER_ANSI,
		.rabbitmq_c		= AMQ_VERSION_STRING,
		.libuuid		= "@UUID_VERSION_STRING@",
	},
	.agent = {
		.version		= NIMRODG_VERSION_STRING,
		.protocol		= NIMRODG_XSTR(NIMRODG_VERSION_MAJOR),
		.platform		= "@NIMRODG_PLATFORM_STRING@",
		.description	= "Nimrod/G Agent, version " NIMRODG_VERSION_STRING " (@NIMRODG_PLATFORM_STRING@)",
		.user_agent		= "NimrodG/" NIMRODG_VERSION_STRING " (@NIMRODG_PLATFORM_STRING@; git:@GIT_DESCRIBE@)",
		.dev_null		= NIMRODG_DEVNULL,
	}
};

